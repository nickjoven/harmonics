__pycache__/
*.pyc

# ket substrate: commit the ledger (CAS + log + config + manifest), ignore
# the Dolt SQL index (derived state — rebuilds via `ket repair`).
.ket/ket.db/

# Rust build artifacts
seed/target/
models/
engine/

# no thanks
CLAUDE.md
external/
