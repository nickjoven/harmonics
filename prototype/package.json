{
  "name": "harmonics-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "Metronome wall + devil's staircase pedagogical prototype.",
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8080",
    "smoke": "node test/smoke_engine.mjs",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "lighthouse": "lhci autorun --config=./lighthouserc.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@lhci/cli": "^0.14.0"
  },
  "license": "MIT"
}
