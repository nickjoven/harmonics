# Path closures — iteration 3

## What this file is

Third iteration of close-paths mode, applying eliminate/recognize
to the four remaining substantive items after iter 2.

| Item | Region | Verdict |
|---|---|---|
| **D.3 articulation** (a, b, c) | D | **FULLY RECOGNIZED — Class 5 / Survives** |
| **D.1** Klein π_1 generators | D | **PARTIAL RECOGNIZE** (cosmological assignment forced; particle still open) |
| **Path (c)** Cross-sector tongue identification | B | **REDUCES** to shared Z_6 lattice structure |
| **Region C** Numerology count | C | **CANNOT CLOSE** by structural reasoning; empirical test required |

The headline result: **D.3 promotion closes anchor obstruction #5**
— sector decoupling is structurally derived from existing
framework content. This is one of the framework's deepest open
questions, now closed by recognizing what's already in
`continuum_limits.md` + `continuity_in_K_nulls.md` N11 +
`path_a_walkthrough.md`.

## Closure 9 — D.3 articulation: FULL RECOGNITION

### The three articulations

Per `path_closures_iter2.md` D.3 promotion, three articulations
were needed for Class 5:

(a) K=1 is structural boundary, not coincidence
(b) No smooth K-running connects K=1 to K<1 (uses N11)
(c) Two anchors set scales independently

Iteration 3 inspection: **all three are already in the framework.**

### (a) K=1 as structural boundary — RECOGNIZED

`continuum_limits.md` Part I §1 derives:

> The discrete Stern-Brocot self-consistency `r = Σ g(p/q)·w(p/q,K)·e^{2πi(p/q)}` at K=1 with `w(p/q, 1) ~ c/q²` (theorem about standard circle map at criticality) summed against Farey 1/q² measure converges to Lebesgue and the sum becomes the Kuramoto self-consistency integral.

The "K=1 critical" is a known mathematical property of the
standard circle map (Jensen-Bak-Bohr): devil's staircase
measure saturates to 1 at K=1, fat-fractal complement of
dimension ~0.87. **This is a structural fact**, not a coincidence
— it follows from the saddle-node bifurcation onset that the
framework's parabola primitive (P4) directly invokes.

Articulation (a) is **already structural**, recognized from
existing primitives + circle map theory.

### (b) No smooth K-running connects K=1 to K<1 — RECOGNIZED via N11

`continuity_in_K_nulls.md` N11 establishes:

> Tongue coverage `w(K) = tongue_coverage_q6(K) / max_coverage`
> caps at 0.138 for K < 1, then jumps discontinuously to 1.0 at
> K = 1.

This discontinuity at K=1 is precisely the non-smooth behavior
articulation (b) requires. The framework's tongue-coverage
formalism CANNOT smoothly interpolate between K<1 and K=1; the
critical line is a structural boundary.

The K=1 → Einstein limit and K<1 → Schrödinger limit
(`continuum_limits.md` Part I + Part II) cannot be smoothly
connected via K-running because the underlying tongue coverage
fails N11 at K=1. **Articulation (b) is N11 as a positive
structural result.**

### (c) Two anchors set scales independently — RECOGNIZED via path_a + reframe

`path_a_walkthrough.md` established the prime-5 obstruction: the
canonical register's prime support {2, 3} cannot reach
15 = 3·5 for v/M_P closure. Combined with
`hierarchy_problem_translation.md` (SM hierarchy doesn't
translate; two-anchor minimum upgrades to structural feature),
the two anchors (H_0 cosmological, v_EW particle) are
**structurally independent inputs**, not derivable from one
another.

Per the disambiguation pattern (`vocabulary_is_the_work_pattern.md`
Instance 6 + Consequence 1), the two-anchor minimum is read as
a structural feature: each anchor pins the scale of its
respective continuum-limit regime independently.

Articulation (c) is **already structural**, recognized from
path_a closure + anchor reframe.

### Combining the three articulations

The composite argument:

1. The framework's substrate self-consistency equation has TWO
   distinct continuum limits at TWO distinct K-regimes (K=1 →
   Einstein, K<1 → Schrödinger) per `continuum_limits.md`.

2. The K=1 critical line is a structural boundary (Jensen-Bak-Bohr
   devil's-staircase saturation, from circle-map criticality
   inherited from primitive P4).

3. The K=1 ↔ K<1 transition is non-smooth (N11 tongue-coverage
   discontinuity).

4. Each regime is anchored independently by its physical scale
   (H_0 for cosmology at K=1; v_EW for particle at K<1).

Therefore: **the cosmological and particle sectors are
structurally decoupled** because they live in distinct,
non-smoothly-connected K-regimes of the substrate's
self-consistency equation, each requiring its own anchor.

This IS the closure of anchor-count obstruction #5. **D.3
promotes to Class 5 / Survives.**

### Verification against Z1-Z3

- **Z1** (numerical match): structural argument has no specific
  numerical prediction; matches qualitative observation that
  cosmology and particle physics use independent anchors. ✓
- **Z2** (no un-derived O(1) factors): no fitted constants in the
  argument. ✓
- **Z3** (only structural inputs): primitives (parabola, mediant,
  fixed-point), Klein topology, circle-map mathematics — all
  framework-internal. ✓

All three pass. Class 5 / Survives.

### What this changes about the framework's status

Anchor-count obstruction #5 (`anchor_count_audit.md` "Structural
decoupling of sectors") was the ONLY remaining obstruction
flagged as "most likely to NOT disambiguate to a feature" per
`vocabulary_is_the_work_pattern.md` Consequence 1. Closure
removes the last open structural question about the two-anchor
status.

Combined with the prior anchor reframe:
- Obstruction #1 (v/M_P): structural feature (path_a closure)
- Obstruction #2 (Fibonacci-depth EW): structural feature (same)
- Obstruction #3 (ω₀ substrate): structural feature
- Obstruction #4 (coordinate/frame split): structural feature
- **Obstruction #5 (sector decoupling): NOW CLOSED structurally**

Framework's two-anchor minimum is now COMPLETELY STRUCTURAL,
with all five obstructions either reframed as features or
derived. The "two-anchor minimum is a feature, not a defect"
reading is fully supported.

## Closure 10 — D.1 Klein π_1 generators: PARTIAL RECOGNIZE

### What recognition surfaced

The framework's existing dynamics machinery
(`klein_topological_keff.py`) shows:

- Klein-bottle Kuramoto with x-direction antiperiodic twist
  gives `|r| ≡ 0`
- Cosmological derivation (`continuum_limits.md` Part I) requires
  `|r| ≠ 0` for Einstein equations via Kuramoto self-consistency

Therefore: **cosmological sector CANNOT live on the
orientation-reversing Klein loop** (the x-twist loop). It MUST
live on the orientation-preserving loop (the y-direction
periodic loop).

This is half of the D.1 assignment forced structurally.

### What remains for full Class 5

Still open: explicit derivation that **particle sector lives on
the orientation-reversing loop** (or equivalently, that the
gauge structure structurally requires the Klein twist).

Hint: `klein_topological_keff.py` gives `K_eff = K_0/2` from
twist, which is subcritical — consistent with K_STAR ≈ 0.86 for
particle sector. But "consistent" is weaker than "forced."

### Verdict

**PARTIAL RECOGNITION**. One half of D.1 (cosmological → no-twist
loop) is forced by existing framework content. The other half
(particle → twist loop) is consistent but not yet forced.

D.1 reduces to: derive that gauge structure requires Klein
twist. Single derivation; multi-session if needed.

## Closure 11 — Path (c) Cross-sector tongue identification: REDUCES

### Original hypothesis

Cosmological tongue derivations and particle tongue derivations
share specific p/q rationals; this shared structure gives
cross-sector unification (potentially closing v/M_P).

### What the existing framework shows

The Z_6 = Z_{q_2 q_3} lattice appears in BOTH sectors:

- **Cosmological**: `baryon_fraction.md` uses Klein-antipodal Z_2
  rep on Z_6 with coprime-to-6 filter to derive Ω partition
  13:5:1/19
- **Particle**: `down_type_double_cover_closed.md` uses S_3 orbit
  dimensions on Z_2 × Z_3 = Z_6 to derive down-type factor 6;
  `item12_K_star_closure.py` uses Klein parity on Fibonacci shift
  for up-type factor 9

The SAME Z_6 LATTICE drives both sectors' Klein-antipodal
eigenmode counting. The difference is which FILTER selects modes:
coprime-to-6 in cosmology vs S_3 orbit in particle.

### What this gives

Cross-sector commonality IS the shared Z_6 lattice structure. The
framework already has structural cross-sector content, just not
in the form path (c) was originally framed.

But: this DOESN'T independently close v/M_P (the prime-5
obstruction from `path_a_walkthrough.md` blocks reaching 15 = 3·5
from Z_6 = Z_{2·3}). The shared lattice gives QUALITATIVE
cross-sector structure but not the SPECIFIC `13⁻¹⁵` quantitative
match.

### Verdict

**REDUCES to existing shared Z_6 lattice structure.** Path (c)'s
cross-sector unification is already in the framework as the
shared Klein-antipodal eigenmode catalog. The v/M_P near-match
remains Class 2 per `path_a_walkthrough.md` (prime-5 obstruction).

Removed from open paths; recognized as existing framework
structure.

## Closure 12 — Region C: CANNOT CLOSE by reasoning alone

### Why structural reasoning doesn't suffice

The numerology count's purpose is to test whether the framework's
1-3% near-match cloud is statistically expected by pigeonhole or
anomalously dense. This is fundamentally an EMPIRICAL question
about the density of small-integer expressions in a given range.

Structural arguments:
- (Cloud is pigeonhole): framework's 6 fundamental integers
  generate ~10²-10³ small-ratio expressions. Many will land
  within 3% of various physical observables by density alone.
  Expected.
- (Cloud is signal): framework's specific load-bearing
  derivations match cleanly (Survives entries). The Floor cloud
  at 1-3% suggests a missing fractional-correction mechanism.

Both arguments have merit. WITHOUT empirical count, can't
distinguish.

### Verdict

**Cannot close in eliminate/recognize mode.** Region C requires
empirical work (the count itself, Phase B). Stays open.

## Updated open list after iteration 3

### Region D: 1 ready + 1 partial + 0 closed

| Status | Item |
|---|---|
| Class 5 / Survives | **D.3 (sector decoupling)** — anchor obstruction #5 CLOSED |
| Partial recognize | D.1 (Klein π_1) — cosmological assignment forced; particle assignment one derivation away from Class 5 |

### Region B: 0 substantive open

| Status | Item |
|---|---|
| Reduces to shared Z_6 | Path (c) — cross-sector lattice commonality already in framework; v/M_P still Class 2 |
| Other paths | Closed in iter 1-2 |

### Region C: 1 empirical work item

| Status | Item |
|---|---|
| Cannot close by reasoning | Region C numerology count — Phase B (empirical) is the work |

## Net effect across all three iterations

| Pre-iteration | After iter 1 | After iter 2 | After iter 3 |
|---|---|---|---|
| Region D: 6 candidates | 3 candidates | 1 substantive + 1 ready | **D.3 Class 5; D.1 partial** |
| Region B: 5 paths | 4 paths | 1 substantive | **0 substantive (Path (c) reduces)** |
| Region C | Phase B ready | Phase B ready | **Empirical work required** |

Net: 11 candidate-paths started → 1 Class 5 closure (D.3) + 1
partial recognition (D.1) + 1 empirical-work item (Region C).
**8 candidates eliminated/blocked/reduced** with structural
reasons across three iterations.

## Headline result

**Anchor-count obstruction #5 closes structurally** via D.3
promotion. Combined with the prior reframe of obstructions
#1-#4, the framework's two-anchor minimum is now FULLY
STRUCTURAL. All five obstructions either reframed as features
or derived.

This is a major framework-status result. The "anchor count is
two; this is structural, not a defect" reading is now complete.

## What remains

Three concrete work items, much smaller than pre-iteration
landscape:

1. **D.1 final assignment** — derive that particle gauge structure
   structurally requires the Klein twist. Single derivation,
   multi-session if needed. Would close D.1 to Class 5 also.

2. **Region C numerology count** — empirical Phase B. Binary
   verdict on Floor calibration.

3. **Update `framework_status.md`** to reflect anchor-count
   obstructions #1-#5 all closed/reframed; remove "anchor count
   reduction" from open structural questions.

(3) is mechanical and follows directly from this iteration's
findings.

## Cross-references

- `path_closures_iter1.md`, `path_closures_iter2.md` — prior
  iterations
- `sector_decoupling_phase_a.md` — Region D Phase A; D.3 now
  Class 5
- `continuum_limits.md` — D.3 articulation (a) source
- `continuity_in_K_nulls.md` N11 — D.3 articulation (b) source
- `path_a_walkthrough.md` — D.3 articulation (c) source
- `vocabulary_is_the_work_pattern.md` — anchor reframe context
- `anchor_count_audit.md` — obstructions #1-#5; needs update
  to reflect #5 closure
- `klein_topological_keff.py` — D.1 partial recognition source
- `baryon_fraction.md`, `down_type_double_cover_closed.md` —
  Path (c) shared Z_6 source
- `framework_status.md` — Active multi-session section; needs
  update
- `numerology_count_phase_a.md` — Region C; remains as the
  active empirical work

## Status

Iteration 3 complete. **D.3 closes Class 5 (anchor obstruction
#5).** D.1 partial; Path (c) reduced; Region C still empirical.
Open list now: D.1 closure derivation + Region C empirical count.
The framework's anchor-count question is structurally resolved.
