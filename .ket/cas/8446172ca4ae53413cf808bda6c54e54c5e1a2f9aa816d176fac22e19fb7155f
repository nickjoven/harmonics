# Ω_b residual audit: passes substrate-side test, does NOT dissolve

## What this file is

A focused audit of the Ω_b 6.7% / 11σ Floor residual under the
disambiguation lens applied to A_s in
`a_s_g1_closure_attempt.md`. The result is informative
**asymmetry**: A_s dissolved (anchor-side category statement);
**Ω_b does not dissolve — it is genuinely substrate-side and
constitutes the framework's first remaining structural gap of
the post-disambiguation work map**.

This sharpens `remaining_gap_shapes.md` Shape A: A.1 (A_s)
closes as category, A.2 (Ω_b) opens as real work, A.3
(Ω_c/Ω_b) inherits as derivative.

## Disambiguation test applied to Ω_b

Per `vocabulary_is_the_work_pattern.md` §"The disambiguation
test", four steps:

### Step 1 — Where does the framing language come from?

The Ω_b residual is framed in terms of:

- **Boundary weight `w`** (`boundary_weight.md`)
- **Klein-eigenmode width asymmetry** (`baryon_fraction.md`)
- **Decoherence tax `1−|r|`** (D33, `baryon_fraction.md`)
- **Partition-denominator correction `δ(w)`**
- **z-dependent w from a_0(z)** (D8, framework-derived)

All five candidates are framework-internal vocabulary. There
is no imported "naturalness," no "horizon-crossing
amplification," no Wilsonian RG. The residual lives entirely
in framework language.

### Step 2 — Does the framework have one unambiguous correspondent?

The framework has **one** prediction for Ω_b:

```
Ω_b = (Klein-singlet ∩ coprime-to-6 mode count) / |F_7|
    = 1 / 19
    = 0.0526
```

Components:
- 1 = the unique mode `ψ_+(1, 5)` per
  `omega_partition_combinatorial.md`
- 19 = `|F_7|` per the Farey count at depth 7

Both are **pure integers** from substrate combinatorics. No
k-Ω map (unlike A_s where q_pivot = F_21 is set by observed
k*). No anchor-derived ingredient. No H_0. No v_EW.

Single, unambiguous correspondent. No K-zoo to disambiguate.

### Step 3 — Restate framework-natively, does it remain a problem?

Restated: "the framework's structural prediction Ω_b = 1/19
differs from the observed Ω_b·h²-derived value by 6.7% at
11σ."

**The discrepancy survives the restatement.** It is a quantitative
disagreement between a framework-derived dimensionless
prediction and an observed dimensionless quantity. No
anchor-side amplification is implicit (compare to A_s, where
the static framework prediction differs from the
inflation-amplified observable by exactly the missing
amplification factor).

### Step 4 — Framework-internal restatement IS the work

The five candidate corrections (C1-C5 from
`omega_b_residual_phase_a.md`) are all *framework-internal
mechanisms* that could close the gap:

- C1 (decoherence tax): partial closure ~3.2%
- C2 (Klein-eigenmode width asymmetry at finite K)
- C3 (sub-partition at fractional depth): weak per Phase A
- C4 (partition-denominator correction): tunable
- C5 (z-dependent w from a_0(z) framework prediction)

None are anchor imports; all are internal-derivation candidates.
The framework-internal restatement IS the actual work. The gap
does not reduce to a category statement.

## Why Ω_b is structurally different from A_s

Direct comparison of the two Floor residuals:

| Property | A_s | Ω_b |
|---|---|---|
| Framework prediction | `(1−φ⁻⁴)/(4·λ·φ·q_pivot²)` | `1/19` |
| Inputs | φ, λ_unlock, q_pivot | 1, 19 (pure integers) |
| Anchor-derived input? | YES (q_pivot via k* in Mpc) | **NO** |
| Implicit amplification convention? | YES (static vs inflation-amplified) | **NO** |
| Disambiguation test | Dissolves (anchor-side amplification) | **Survives (substrate-side)** |
| Closure path | Requires substrate-side inflation derivation | **Internal mechanism C1-C5** |

The asymmetry is structurally precise: A_s is a derived
*amplitude* (curvature perturbation variance), and amplitudes
in cosmology carry inflationary amplification factors that
require absolute time scale (anchor). Ω_b is a derived
*ratio* (energy density fraction), and ratios within the
framework's substrate are pure combinatorics (Farey, Klein,
Z₂-rep) with no scale dependence at all.

This is a specific case of the framework's broader structure:
**dimensionful predictions** require anchor input; **dimensionless
ratios** can be framework-pure. Ω_b is dimensionless and
substrate-pure; A_s is a dimensionless amplitude that
inherits anchor dependence through its inflation amplification
convention.

## What this does to the work map

`remaining_gap_shapes.md` listed three Shape-A items: A_s, Ω_b,
Ω_c/Ω_b. After this audit:

- **Shape A.1 (A_s)** — closes via disambiguation as category
  statement. *Not a derivation gap.*
- **Shape A.2 (Ω_b)** — does NOT close via disambiguation. **The
  first genuine remaining substrate-side Floor.** Real
  structural work.
- **Shape A.3 (Ω_c/Ω_b)** — derivative of Ω_b via matter
  conservation. Closes when Ω_b closes. Not independent.

The framework's substantive open structural list now reads:

1. **Ω_b 6.7% Floor** (Shape A.2) — internal mechanism among
   C1-C5.
2. **Anchor obstructions #2-#5** (Shape C) — re-audit candidates;
   most expected to dissolve to features per the path (a)
   pattern; #5 (sector decoupling) most likely to be a real
   open question.
3. **Cross-sector v/M_P** (Shape D) — closed via path (a) but
   Shape D opens specific paths (c), (d) that haven't been
   tested.

Of these, **Ω_b is the most concrete because it has explicit
candidate mechanisms framed framework-natively** with five
specific options to test. The others either dissolve (A_s,
likely #2-#3) or require new structural derivations (#5,
paths c/d).

## Ranking the five candidates for next-session work

Per `omega_b_residual_phase_a.md` §5 ranking:

### C5 — z-dependent w from a_0(z) framework prediction (most leverage)

- **Mechanism**: the framework predicts a_0(z) = c·H(z)/(2π).
  Ω_Λ, Ω_DM, Ω_b are observed at different cosmological scales
  (CMB, galaxy clusters, BBN). If the framework's `w` runs
  with z, the three implied weights (w_Λ = 0.828, w_b = 0.926,
  w_DM = 0.957) could all be *consistent* — describing one
  function w(z) sampled at three different z's.
- **Anchor status**: a_0(z) shape is framework-derived (uses
  Ω_m, Ω_Λ as framework dimensionless ratios). Absolute
  normalization uses H_0 but cancels in the cross-observable
  comparison. **Substrate-side mechanism.**
- **Test**: compute z_eff for each of {Ω_Λ, Ω_b, Ω_DM} and
  check whether w(z_eff) is a monotone function. If yes, the
  three implied weights are consistent and the residual closes
  structurally.
- **Effort**: medium. Requires identifying the dominant z-scale
  for each Planck observation and computing w(z) from the
  framework's a_0(z).

### C2 — Klein-eigenmode width asymmetry at finite K (structural)

- **Mechanism**: the Z₂ decomposition `ψ_+(1,5)` (baryonic) and
  `ψ_-(1,5)` (dark) assumes equal tongue widths at idealized
  K=1. At finite K (or finite-N corrections), the antiperiodic
  twist introduces sym/antisym asymmetry — leakage from
  baryonic to dark.
- **Anchor status**: pure framework-internal (Klein flip on
  6-point lattice).
- **Test**: compute the finite-K correction to the Z₂-eigenmode
  tongue widths using `boundary_weight.py` machinery. Verify
  whether the asymmetry magnitude reaches ~6.7%.
- **Effort**: medium-light. Reuses existing Klein-flip code from
  the down-type derivation that closed similarly.

### C4 — Partition-denominator correction δ(w)

- Tunable; less structural. Backup if C5 + C2 don't close.

### C1 — Decoherence tax (partial)

- Gets ~half (3.2% of the 6.7%). Combinable with C2 or C5.

### C3 — Sub-partition at fractional depth

- Weakest per Phase A. Drop unless others fail.

## Recommendation

Attack **C5 (z-dependent w)** first. Highest leverage; if it
closes, it does so via a *framework-derived* z-dependence
(a_0(z) is already in the framework), not by introducing new
mechanisms. The closure would be:

- The three observables (Ω_Λ, Ω_b, Ω_DM) sample the substrate
  at different z.
- `w(z)` runs because a_0(z) sets a z-dependent locking scale.
- The three implied weights `{0.828, 0.926, 0.957}` consistently
  trace one function `w(z)`.
- The framework's "Ω_b = 1/19" is the asymptotic z → 0 value;
  the observed Ω_b·h² samples a different z and gives the
  observed value via the same `w(z)`.

If C5 closes, the Ω_b residual becomes a structural derivation
rather than a Floor — Class 5 / Survives.

If C5 doesn't close, fall back to C2 (Klein asymmetry) for the
sym/antisym leakage mechanism.

If neither closes, the Ω_b residual stays as a genuine Floor —
the framework's substrate-side prediction is `1/19` and the 6.7%
gap reflects either (a) a missing structural mechanism or (b)
PDG observational shifts. This is the honest landing.

## Framework status implications

`framework_status.md` Floor section needs revision:

**Current (post A_s audit)**: A_s entry has reframe note pointing
to anchor-side amplification reading. Ω_b and Ω_c/Ω_b entries
remain as "Floor structural residual."

**Post this audit**: Ω_b entry is **the framework's first
genuine remaining substrate-side structural gap**. Should be
flagged as "active open work" with C5 as the recommended probe,
and the audit concluding the disambiguation pattern doesn't
apply to this entry.

This is positive content for the framework's status: it
identifies a SHARP open question (C5 test) rather than a
diffuse "Floor that might close someday."

## Cross-references

- `baryon_fraction.md` — Ω_b = 1/19 derivation
- `omega_b_residual_phase_a.md` — three-way inconsistency, C1-C5
- `omega_b_residual_phase_b.md` — hybrid `(1/19)·|r|²`
  attempt (Class 2)
- `boundary_weight.md`, `boundary_weight.py` — w-formalism
- `a_s_g1_closure_attempt.md` — the A_s closure that landed
  category-side
- `vocabulary_is_the_work_pattern.md` — pattern this audit
  applies and finds NOT to apply here
- `framework_status.md` — Floor section needing revision
- `remaining_gap_shapes.md` Shape A — work map updated
