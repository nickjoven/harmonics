# Born rule |ψ|² is parameter-free at the binary — pressed

Pressing the discriminator from `thread_chronology.md` #TICK: does
`|ψ|²` emerge **parameter-free** from saddle-node geometry (the
framework's deterministic reading), or does it need a noise scale
(the stochastic-Kramers competitor)? Pressed to a verdict.

**Result: parameter-free and noise-free at the fundamental binary,
conditional only on `#TICK(b)`.** The discriminator against the
stochastic competitor is won; `#TICK(a)` closes as a by-product;
all residual risk collapses into the single `#TICK(b)` question.

## The exponent — clean, parameter-free, deterministic

`born_rule.md` + `born_rule_tongues.py`: every Arnold-tongue
boundary is a saddle-node; `Δθ ∝ √ε` (numeric, 6 decimals) ⇒
`P ∝ Δθ² ∝ |ψ|²`. The exponent `2` is the degree of the parabola
normal form `x² + μ = 0` — the universal codimension-1
bifurcation on S¹, structurally stable, no other generic exponent.
**No `kT`, no barrier height, no Gaussian width appears.**

Discriminator vs. stochastic Kramers:

| | exponent origin | free scale | near-boundary law |
|---|---|---|---|
| Saddle-node (framework) | normal-form degree (topological) | **none** | `P ∝ ε`, `T`-independent |
| Kramers (competitor #2) | Arrhenius `exp(−ΔU/kT)` | `kT`, `ΔU` | `T`-dependent |

The framework's version is the deterministic one and is already
the numerically-verified one.

## The two places a parameter could hide — both are primitives, not parameters

**Gap A — uniform curvature.** `born_rule.md` flags `α_k = α`
("fair detector") as Open. At the **fundamental binary** this is
not an assumption: `ẋ = μ − x²` is *even in x*, so its two
branches `±√μ` are exact mirror images ⇒ identical curvature by
parity. That parity *is* the half-twist `J² = −I` Z₂-involution
(kink ↔ antikink; `substrate_determinism.md` inviolable #7,
half-twist `= π` exactly; #1 topological-charge conservation). So
`born_rule.md`'s multi-outcome curvature worry **does not arise at
the binary** — the Born rule is *most* rigorous exactly at the
fundamental tick and only acquires the "Open" caveat in the
*derived* N>2 regime. Closed for the binary, by an inviolable.

**Gap B — the incoming measure.** `P ∝ |ψ|²` needs the incoming
ensemble uniform in the natural coordinate. `born_rule.md` says
"Liouville." Pressed: the substrate's native measure is the
**mediant counting measure** (the integer/counting primitive —
each Stern-Brocot child one discrete node, pre-EML reweighting);
the continuum "Liouville" phrasing is its *shadow*. Not a fitted
parameter — it is the discreteness commitment the framework makes
everywhere. **But** "the discrete mediant counting measure is
constructively the source of the apparent continuum measure" is
asserted, not constructed — and that is *exactly* `#TICK(b)`.

## Verdict

- The `|ψ|²` exponent is parameter-free, deterministic, noise-free
  — the discriminator against stochastic Kramers (#2) is **won,
  conditionally**: the two free-looking inputs are an inviolable
  (J-parity) and a primitive (mediant counting), not parameters.
- **`#TICK(a)` closes as a by-product**: the per-tick Z₂ sign *is*
  the saddle-node branch; the deterministic selector is which side
  of the J-symmetric separatrix the mediant-counted incoming state
  lies on. No noise needed.
- **All residual risk collapses into `#TICK(b)`**: whether the
  discrete mediant counting measure is *constructively* the source
  of the apparent continuum measure (= the first-order →
  second-order / continuum-is-shadow constructiveness question).
- Honest scope: this is *cleaner* than `a1_from_saddle_node.md`'s
  mass identity — the `|ψ|²` exponent does **not** depend on that
  doc's chosen `μ_center = w` convention (which only scales the
  absolute `a_1`, not the Born exponent). No overclaim by
  association.

Net: "two vague gaps + a live competitor" → "competitor
discriminated, `#TICK(a)` closed, one precise question
(`#TICK(b)`) carrying all residual risk."

## Status

Class 3 (resolution by structural identification + precisely
located residual). No new primitive. Sharpens `born_rule.md`'s
"Open" (uniform curvature) — closed for the binary by the
half-twist Z₂-involution. Does **not** close `#TICK(b)`; it
concentrates the entire residual there.

## Cross-links

- `born_rule.md` — the saddle-node `|ψ|²` derivation; its "Open"
  uniform-curvature item is closed here *for the binary* (parity =
  J Z₂-involution).
- `a1_from_saddle_node.md` — same parabola primitive; note the
  Born exponent is independent of that doc's `μ_center = w`
  convention.
- `substrate_determinism.md` — inviolables #1, #7 (the J-parity
  that forces uniform curvature at the binary).
- `thread_chronology.md` — #TICK: (a) closed here; (b) carries the
  whole residual; the stochastic competitor is discriminated.

## One-line summary

The Born `|ψ|²` exponent is parameter-free and noise-free
(topological saddle-node degree, no `kT`), uniform-curvature is
*forced* at the binary by the half-twist `J²=−I` parity, the
incoming measure *is* the mediant counting primitive — so the
deterministic reading beats stochastic-Kramers and `#TICK(a)`
closes, with the entire residual collapsed into the single
`#TICK(b)` constructiveness question.
