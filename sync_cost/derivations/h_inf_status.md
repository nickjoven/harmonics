# H_inf status — anchor-side, scale-free in framework integers

## What this file is

Status note consolidating the framework's position on H_inf
(Hubble parameter during inflation). Created 2026-04-26 to anchor
cross-references from `anchor_count_audit.md`,
`a_s_g1_closure_attempt.md`, `framework_status.md`,
`hierarchy_gaussian_lattice.md`, and `vocabulary_is_the_work_pattern.md`.
Substantive content lives in those files; this is a pointer.

## Status

**H_inf is anchor-side.** The framework computes H_inf in
framework-integer units (scale-free), but the absolute value
requires the cosmological H_0 anchor for conversion to physical
units (GeV, kg, etc.).

## Why anchor-side

H_inf is a dimensionful quantity (energy / time scale during
inflation). Per `coupling_scales.md` and the dimensional
analysis in `hierarchy_gaussian_lattice.md`, dimensionful
cosmological scales require the H_0 anchor; the framework's
substrate provides ratios but not absolute units.

The framework's "scale-free reframe" of H_inf gives the
framework-integer expression for the **ratio** H_inf/M_P (or
related dimensionless combinations), but not the absolute value
of H_inf alone.

## Implication for A_s closure

Per `a_s_g1_closure_attempt.md`, the inflation amplification
factor f_amp = (H_inf/M_P)²/(8π²·ε·c_s) requires both H_inf
and ε. Both are anchor-side:

- H_inf: dimensionless ratio framework-derivable; absolute scale
  requires H_0
- ε (slow-roll): requires Fibonacci-depth → e-fold conversion
  = absolute time anchor (anchor obstruction #3)

Hence the framework declines to predict f_amp's specific value.
This supports `vocabulary_is_the_work_pattern.md` Instance 7
closure: A_s = 2.33×10⁻⁹ substrate-side prediction is complete;
the 11% gap to A_s_obs is anchor-side.

## Implication for anchor count

Per `anchor_count_audit.md`, H_inf belongs to the cosmological
anchor side (covered by H_0). The "scale-free" reframe declared
that the framework's substrate computation gives H_inf in
framework-integer terms up to the cosmological anchor — which
this audit confirms.

The framework requires **two** anchors (H_0 cosmological + v_EW
particle); H_inf is on the cosmological side, derivable from H_0
via framework-integer ratios.

## Status under framework_status.md categorization

`framework_status.md` Eliminated section lists "H_inf from
framework integers" as **out of class** (scale-free reframe).
The framework provides ratios, not absolute dimensional values.
This is consistent with the two-anchor minimum (D.3 closure):
absolute scales require anchors; the framework correctly
declines to predict them.

## Cross-references

- `a_s_g1_closure_attempt.md` — H_inf role in A_s amplification
- `anchor_count_audit.md` — H_inf as cosmological-anchor-side
  scale; obstruction #3 = no framework-native ω₀
- `coupling_scales.md` — dimensional anchor requirements
- `hierarchy_gaussian_lattice.md` — scale-free framework-integer
  identities for cosmological hierarchy
- `framework_status.md` Eliminated section — H_inf from framework
  integers is out of class
- `vocabulary_is_the_work_pattern.md` Instance 7 — A_s closure
  uses H_inf-anchor-side reading

## Status

H_inf is anchor-side (cosmological anchor = H_0 covers it).
Framework computes scale-free ratios; absolute value requires
H_0. This is consistent with the two-anchor minimum and with
Instance 7 closure of A_s.

This file is a pointer-doc consolidating cross-references to
substantive content in `a_s_g1_closure_attempt.md`,
`anchor_count_audit.md`, and `coupling_scales.md`. No new
derivation here.
